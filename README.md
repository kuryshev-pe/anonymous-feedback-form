# Анонимная форма обратной связи

Анонимная форма обратной связи - это веб-приложение, позволяющее пользователям отправлять отзывы и предложения без раскрытия своей личности. Приложение обеспечивает безопасную и приватную систему сбора обратной связи для организаций.

## 🌟 Особенности

- **Анонимность**: Отправители не раскрывают свою личность
- **Безопасность**: Защита от CSRF атак с использованием токенов
- **Интуитивный интерфейс**: Простое и понятное пользовательское взаимодействие
- **Кросс-платформенность**: Работает на всех современных браузерах

## 📁 Структура проекта

```
anonymous-feedback-form/
├── backend/           # Django backend приложение
│   ├── feedback/      # Модели, представления и административная панель
│   └── backend/       # Основные настройки Django
├── frontend/          # React фронтенд приложение
│   ├── src/           # Исходный код React компонентов
│   └── public/        # Статические файлы и HTML шаблон
├── helm/              # Helm чарты для развертывания
├── docker-compose.yaml # Docker compose конфигурация
└── API_DOCUMENTATION.md # Документация по API
```

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Node.js (для разработки)

### Локальный запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/your-repo/anonymous-feedback-form.git
cd anonymous-feedback-form
```

2. Запустите приложение с помощью Docker Compose:
```bash
docker-compose up -d
```

3. Откройте в браузере:
   - Frontend: http://localhost:3000
   - Backend Admin: http://localhost:8000/admin

## 🛠️ Разработка

### Фронтенд (React)

```bash
cd frontend
npm install
npm start
```

### Бэкенд (Django)

```bash
cd backend
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

## 📚 API Документация

Для подробной информации о доступных API эндпоинтах, см. [API_DOCUMENTATION.md](API_DOCUMENTATION.md).

### Основные эндпоинты:

- `GET /api/message` - Получить CSRF токен
- `POST /api/feedback` - Отправить отзыв
- `GET /api/error` - Тестовый эндпоинт для проверки ошибок

## 📦 Развертывание

### Docker

Приложение полностью готово к развертыванию с использованием Docker:

```bash
docker-compose up -d
```

### Helm

Для Kubernetes развертывания используйте Helm чарты:

```bash
helm install feedback-form ./helm
```

## 🤝 Вклад в проект

1. Создайте fork репозитория
2. Создайте ветку для ваших изменений (`git checkout -b feature/your-feature`)
3. Зафиксируйте изменения (`git commit -am 'Add some feature'`)
4. Отправьте ветку (`git push origin feature/your-feature`)
5. Создайте pull request

## 📬 Связаться

Если у вас есть вопросы или предложения, создайте issue в репозитории или свяжитесь с нами по электронной почте.
